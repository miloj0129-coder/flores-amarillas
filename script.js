const galaxy=document.getElementById('galaxy');const field=document.getElementById('flowerField');const modal=document.getElementById('modal');const modalMessage=document.getElementById('modal-message');let current=0,dragging=false,moved=false,startX=0,startY=0,rx=-6,ry=7;const messages=["Me encanta la forma en que haces que todo se sienta un poquito más bonito.","Si hoy necesitas una señal, aquí está: eres importante, eres querida y mereces cosas hermosas.","Gracias por estar. Tu presencia tiene esa magia tranquila que hace bien al corazón.","Ojalá pudieras verte como te veo yo: luminosa, especial y absolutamente irremplazable.","Contigo hasta el silencio se siente bonito. Qué suerte coincidir contigo en este universo.","Te guardo un abrazo enorme, de esos que dicen sin palabras: aquí estoy y te quiero mucho.","No tienes que ser perfecta para ser maravillosa; ya eres más que suficiente.","A veces la mejor parte del día es simplemente recordar que existes.","Te elegiría en esta galaxia y en todas las que todavía no conocemos.","Tu forma de ser deja una luz que no se apaga fácilmente.","Que hoy recibas aunque sea una parte de todo lo bonito que tú entregas.","Hay algo en ti que hace que el mundo parezca un lugar más amable."];const types=['sunflower','daisy','rose','sunflower','daisy','rose','sunflower','rose','daisy','sunflower','rose','daisy'];const positions=[['8%','34'],['22%','16'],['37%','68'],['55%','17'],['72%','35'],['86%','60'],['14%','69'],['48%','43'],['65%','73'],['91%','22'],['31%','40'],['78%','72']];function makeFlower(i){const b=document.createElement('button');b.className=`flower botanical ${types[i]}`;b.dataset.message=messages[i];b.setAttribute('aria-label','Abrir mensaje de una flor');b.style.left=positions[i][0];b.style.top=`${positions[i][1]}%`;b.style.setProperty('--scale',(0.62+Math.random()*.45).toFixed(2));b.style.setProperty('--depth',`${Math.round(Math.random()*120-50)}px`);b.style.animationDelay=`${(Math.random()*2.8).toFixed(2)}s`;b.innerHTML='<span class="stem"></span><span class="leaf l1"></span><span class="leaf l2"></span><span class="petal-set"></span><span class="center"></span>';b.addEventListener('click',e=>{if(moved)return;current=i;const r=galaxy.getBoundingClientRect();showMessage(b.dataset.message,e.clientX-r.left,e.clientY-r.top)});field.appendChild(b)}for(let i=0;i<messages.length;i++)makeFlower(i);function setTilt(){galaxy.style.setProperty('--rx',`${rx}deg`);galaxy.style.setProperty('--ry',`${ry}deg`)}function burst(x,y){for(let i=0;i<18;i++){const s=document.createElement('i');s.className='click-sparkle';s.textContent=i%3===0?'✦':'·';s.style.left=`${x+(Math.random()-.5)*90}px`;s.style.top=`${y+(Math.random()-.5)*65}px`;s.style.setProperty('--dx',`${(Math.random()-.5)*150}px`);s.style.setProperty('--dy',`${-40-Math.random()*100}px`);galaxy.appendChild(s);setTimeout(()=>s.remove(),1200)}}function showMessage(text,x=galaxy.clientWidth/2,y=galaxy.clientHeight/2){modalMessage.textContent=`“${text}”`;modal.classList.add('open');modal.setAttribute('aria-hidden','false');burst(x,y)}document.getElementById('surprise').onclick=()=>{current=(current+1)%messages.length;showMessage(messages[current])};document.getElementById('another').onclick=()=>{current=(current+1)%messages.length;modalMessage.textContent=`“${messages[current]}”`};document.getElementById('close').onclick=()=>{modal.classList.remove('open');modal.setAttribute('aria-hidden','true')};modal.onclick=e=>{if(e.target===modal)document.getElementById('close').click()};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal.classList.contains('open'))document.getElementById('close').click()});galaxy.addEventListener('pointerdown',e=>{if(e.target.closest('.flower'))return;dragging=true;moved=false;startX=e.clientX;startY=e.clientY;galaxy.setPointerCapture(e.pointerId)});galaxy.addEventListener('pointermove',e=>{if(!dragging)return;const dx=e.clientX-startX,dy=e.clientY-startY;if(Math.abs(dx)+Math.abs(dy)>6)moved=true;ry=Math.max(-34,Math.min(34,ry+dx*.13));rx=Math.max(-28,Math.min(20,rx-dy*.13));startX=e.clientX;startY=e.clientY;setTilt()});function stop(){dragging=false;setTimeout(()=>moved=false,50)}galaxy.addEventListener('pointerup',stop);galaxy.addEventListener('pointercancel',stop);galaxy.addEventListener('pointerleave',stop);const canvas=document.getElementById('space'),ctx=canvas.getContext('2d');let particles=[];function resize(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;ctx.setTransform(d,0,0,d,0,0);particles=Array.from({length:240},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.8+.25,s:Math.random()*.24+.03,p:Math.random()*7,w:Math.random()>.72}))}function render(t=0){ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of particles){p.y-=p.s;if(p.y<0)p.y=innerHeight;const a=.18+(Math.sin(t*.001+p.p)+1)*.25;ctx.fillStyle=p.w?`rgba(255,231,100,${a})`:`rgba(220,233,255,${a})`;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(render)}resize();addEventListener('resize',resize);setTilt();requestAnimationFrame(render);
